物理层：比特流
链路层：帧
网络层：分组，数据包
传输层：数据段，报文段
应用层：报文

HFC是利用有线电视网络实现网络接入的技术
ADSL是利用现有的电话网络的用户线路实现的接入网络
局域网接入
移动接入网络

OSI参考模型中 协议数据单元(PDU)在不同层有不同的叫法
应用层：报文
传输层：段（数据段或报文段）
网络层：分组或包
数据链路层：帧
物理层：位流或比特流

网络互联层：网络互联层是整个 TCP/IP 参考模型的核心，主要解决把数据分组发往目的网络或主机的问题。网络互联层还包括互联网控制报文协议 ICMP、互联网多播组管理协议IGMP 以及路由协议，如BGP、OSPF 和 RIP 等。

ARPAnet 是第一个分组交换计算机网络，也是当今因特网的祖先。记忆方法：ARPANET的第一个字母是 A，所以是“第一个，祖先”。故本题选 A，其他选项直接排除。

UDP的实现
1. ​**服务质量（QoS）机制**
2. ​**实时传输协议（RTP）/RTCP**
	- 时间戳（解决时序问题）
	- 序列号（解决乱序问题）  
	- 负载类型标识（区分音视频）
	- 服务质量监控（RTCP反馈）
3.  **自适应码率控制**

POP3 110
FTP 服务器默认端口号是 21，
HTTP 服务器默认端口号 80，
SMTP 服务器默认端口号是25，
DNS 服务器默认端口号是 53
![[Pasted image 20251014200324.png]]
#### 计算机网络的定义
分组交换设备可以实现数据分组的接收与转发，是构成internet的重要基础，具有多种形式，最典型的是路由器和交换机。
目前最大的应用最广泛的计算机网络就是internet或称为因特网

#### 什么是协议
计算机网络中的实体在进行数据交换过程中必须遵循规则和约定，这些规则和约定就是网络协议
协议三要素：
1. 语法 定义实体之间交换信息的格式与结构
2. 语义 定义实体之间交换信息中需要发送哪些控制信息，这些信息的具体含义，以及针对不同含义的控制信息，接受信息端该如何响应
3. 时序 定义实体之间交换信息的顺序以及如何匹配/适应彼此的速度

#### 计算机网络的功能
用于主机之间的实现快速的信息交换，实现资源共享，包括
1. 硬件资源共享  云计算/云存储
2. 软件资源共享  软件即服务SaaS
3. 信息资源共享  信息交换
#### 拓扑结构的分类
指的是网络中主机和网络设备之间的物理连接关系和布局
1、总线拓扑、环形拓扑、星型拓扑、网状拓扑、树形拓扑、混合拓扑

#### 计算机网络结构
1、网络边缘：连接到网络上的所有端系统构成了网络边缘
2、接入网络：
3、网络核心：比较典型的分组交换设备是路由器和交换机等。

#### 数据交换技术
数据交换是实现在大规模网络核心上进行数据传输的技术基础。常见的数据交换技术包括：
(1) 电路交换：最早出现的一种交换方式。主要适用于语音和视频这类实时性强的业务。包括3个阶段：建立电路、传输数据和拆除电路。
(2) 报文交换：现在计算机网络没有采用。不适用于实时通信，不得不丢弃报文。
(3) 分组交换（包交换）：目前计算机网络广泛采用的技术。优点：1）交换设备存储容量要求低；2）交换速度快；3）可靠传输效率高；4）更加公平。

#### 时延
时延是评价计算机网络性能的一个重要的性能指标，也称为延迟。
通常将连接两个结点的直接链路称为一个“跳步”，简称“跳”。
时延分类： 
（1）结点处理时延：每个分组到达交换结点时进行的检错、检索转发表等时间总和，常忽略。记dc。
（2）排队时延：分组在缓存中排队等待的时间。大小不确定。记为dq。
（3）传输时延：当一个分组在输出链路发送时，从发送第一位开始，到发送完最后一位为止，所用的时间，称为传输时延，也称为发送时延，记为 dt。设分组长度 Lbit，链路带宽（即速率）Rbit/s，则 dt=L/R。 
（4）传播时延：信号从发送端发送出来，经过一定距离的物理链路到达接收端所需要的时间，称为传播时延。设物理链路长度 Dm，信号传播速度 Vm/s，则 dp=D/V。

#### 传输介质
1、双绞线 
2、光纤
3、无线电波

网络结构包括网络边缘、接入网络、网络核心
多路复用方法：频分多路复用FDM、时分多路复用TDM、波分多路复用WDM、码分多路复用CDM、统计多路复用
数据交换的类型：电路交换、报文交换、分组交换

#### 时延

1. 结点处理时延 每个分组到达交换结点时进行的检错、检索转发表等时间的总和。记dc
2. 排队时延  分组在缓存中排队等待的时间，大小不确定。记dq
3. 传输时延  分组在输出链路发送时，从发送第一位开始，到发送完最后一位为止，所用的事件，称为传输时延，也称为发送时延，记dt
4. 传播时延  新号从发送端发送出来，经过一定距离的物理链路到达接收端所需要的事件，称为传播时延，记dp